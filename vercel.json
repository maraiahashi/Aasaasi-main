{
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://<RENDER_HOST>/api/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
